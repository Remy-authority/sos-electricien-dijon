# tasks/lessons.md — SOS Électricien Dijon

> Format : [date] | ce qui a mal tourné | règle pour l'éviter.
> Relire ce fichier à chaque démarrage de session.

- [2026-07-26] | Sur le site n°2 (Annecy), le Builder avait une URL en ligne comme référence design : impossible de percevoir le motion design, il a cloné le rendu d'Angers | Toujours donner au Builder un CODE SOURCE local comme référence design (ici PROTEC-DARD), jamais une URL. Le contrôle visuel CEO vérifie explicitement « ressemble à PROTEC-DARD adapté » ET « ne ressemble PAS aux sites précédents ».
- [2026-07-26] | `robots.ts` ne protège la production que si `SEO_NOINDEX=1` est posé sur Vercel (sinon un merge sur `main` rend le site indexable) | À la création de chaque projet Vercel du portefeuille : poser immédiatement `SEO_NOINDEX=1` en env Production, et ne le retirer qu'à la mise en ligne validée par Rémy (Étape 6).
- [2026-07-26] | Le plan SEO donnait les codes postaux « de mémoire » : Marange-Silvange était indiqué 57157 au lieu de 57535 | Ne jamais publier une donnée factuelle (code postal, population, nom de commune) sans la vérifier à la source. `geo.api.gouv.fr` (INSEE / La Poste) permet de contrôler toutes les communes en une commande.
- [2026-07-26] | Le disque était plein (638 Mo libres) : `npm install` a échoué à moitié et a laissé le binaire SWC de Next tronqué, ce qui a fait échouer le build avec une erreur trompeuse (« Failed to load SWC binary ») sans rapport apparent avec l'espace disque | Devant un `next build` qui échoue sur le chargement de SWC, vérifier `df -h` en premier, libérer de la place, puis supprimer le paquet natif concerné et relancer `npm i` plutôt que de chercher un problème de configuration.
- [2026-07-26] | Les premières captures Playwright montraient des sections vides : les animations `whileInView` de Framer Motion n'avaient pas eu le temps de se déclencher pendant un défilement trop rapide, ce qui aurait pu être pris pour un bug de rendu | Pour contrôler visuellement un site animé au scroll, défiler par pas d'environ 60 % de la hauteur d'écran avec au moins 350 ms d'attente par pas, et compléter la capture pleine page par des captures écran par écran.
- [2026-07-26] | `bg-sand-50/97` n'existe pas dans l'échelle d'opacité Tailwind : la classe est silencieusement ignorée, le panneau de menu mobile est resté transparent et illisible sans aucune erreur de build | N'utiliser que les paliers d'opacité de l'échelle Tailwind (5, 10, 20… 90, 95, 100) ou la syntaxe arbitraire `/[.97]`. Une classe invalide ne casse jamais le build : seul le contrôle visuel la détecte.
- [2026-07-27] | Les 12 pages communes se partageaient un pool de 3 photos de tête (HERO_POOL) : deux villes affichaient la même image, défaut déjà vu sur les sites précédents et signalé par Rémy | RÈGLE PERMANENTE : une image de tête UNIQUE par commune (public/zones/<slug>.jpg), décor différencié par ville, câblage par slug, jamais de pool partagé. Le contrôle visuel CEO compare les pages communes entre elles avant validation.
- [2026-07-27] | Le Builder a commencé à écrire les 8 prestations et 12 communes avant que `docs/SEO-GEO-PLAN.md` existe, en établissant sa propre liste de pages : slugs et communes divergents du plan livré ensuite, donc renommages et réécritures (2 pages jetées, 6 communes à écrire en plus) | Avant d'écrire le moindre `content/*.json`, vérifier que le livrable amont existe réellement (`ls docs/SEO-GEO-PLAN.md`). S'il manque, prévenir le CEO et commencer par le code et le design, jamais par le contenu : les slugs conditionnent le maillage interne et le calendrier éditorial de l'autoblog.
- [2026-07-27] | `npm install` a échoué sur des droits du cache npm de l'utilisateur (`EACCES` sur `~/.npm`), avec un code de sortie 0 trompeur et un `node_modules/` absent | Devant un `npm install` qui « réussit » sans créer `node_modules`, relire la fin du log : si le cache est en cause, relancer avec `npm install --cache /private/tmp/<dossier>` plutôt que de tenter un `sudo chown` sur le dossier personnel.
- [2026-07-27] | Le contrôle visuel CEO en Playwright headless a d'abord capturé 48 fois la page de connexion Vercel : le lien de partage `_vercel_share` collé par un humain ne marchait plus (chaque régénération invalide le jeton précédent), et rien dans le script ne le détectait | Pour contrôler une preview protégée : régénérer un jeton frais via l'outil Vercel juste avant la campagne, vérifier `page.title()` après le premier goto (si « Login – Vercel », STOP), et seulement ensuite lancer les captures.
- [2026-07-27] | Le CEO a affirmé de mémoire qu'OVH incluait des redirections email gratuites avec le domaine : faux (offre supprimée pour les nouveaux domaines), Rémy a dû corriger. La bonne réponse était dans les DNS des sites précédents du portefeuille : ForwardEmail (mx1/mx2.forwardemail.net + TXT forward-email=contact:...) | Pour toute question d'infrastructure récurrente du portefeuille (email, DNS, analytics...), interroger d'abord ce que les sites déjà en ligne utilisent (`dig MX/TXT <domaine>`) au lieu de répondre de mémoire : le portefeuille EST la documentation.
- [2026-07-27] | Deux vestiges de vocabulaire plomberie (« ne s'écoule plus », « Qu'est-ce qui est bouché ? ») ont survécu au nettoyage et aux vérifications du Builder, parce que le grep de contrôle ne cherchait que les mots du métier source (débouchage, canalisation) | Lors d'une duplication vers un autre métier, faire un balayage lexical du CHAMP SÉMANTIQUE du métier source (verbes et images : écouler, boucher, refouler...), pas seulement de ses mots techniques, et contrôler visuellement les pages transactionnelles (contact, formulaire) où ce vocabulaire se niche dans le code des composants plutôt que dans content/.
- [2026-07-27] | Achat du numéro du site : aucun 09 en stock chez Twilio FR, et la tentative de repli sur un 03 39 géographique a échoué avec l'erreur 21649 (le bundle réglementaire approuvé du compte est de type « National », les 01-05 exigent un dossier « Local » avec adresse en zone, les mobiles ne sont pas vendus) | Sur ce compte Twilio, seuls les 09 sont achetables ; le stock est intermittent : filtrer `AvailablePhoneNumbers/FR/Local.json` sur `+339` (l'endpoint « National » n'existe pas) et poser une surveillance d'achat automatique plutôt que d'attendre à la main. Recettes complètes : `docs/INFRA-PORTEFEUILLE.md`.
- [2026-07-27] | Après remplacement de `public/persona.jpg` par un nouveau visuel portant le MÊME nom de fichier, le contrôle visuel local montrait toujours l'ancienne image : `next start` servait la version optimisée déjà en cache | Après avoir écrasé une image de `public/` sans changer son nom, supprimer `.next/cache/images` avant le contrôle visuel, sinon on valide l'ancien visuel. En production Vercel, un nouveau déploiement régénère le cache, le piège est local.
